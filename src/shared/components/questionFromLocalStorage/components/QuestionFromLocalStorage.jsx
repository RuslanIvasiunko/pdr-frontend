import s from './styles.module.scss';

const QuestionFromLocalStorage = ({
  questions,
  questionIndex,
  handleAnswerClick,
  setCurrentQuestionIndex,
}) => {
  return <div></div>;
};

export default QuestionFromLocalStorage;
