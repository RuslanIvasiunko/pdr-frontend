import { Navbar } from '@/shared/components/Navbar';

import s from './styles.module.scss';

const Header = () => {
  return (
    <header>
      <Navbar />
    </header>
  );
};

export default Header;
